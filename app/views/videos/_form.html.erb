 <div class="row form video-border">

<h5><span class="typcn typcn-video-outline"></span> New Video / Music Link</h5>

<%= form_for video do |f| %>

<% if video.errors.any? %>
The following errors were found while submitting the form:
      <ul>
        <% video.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
<% end %>

        <p><%= f.label :link %>
        <%= f.text_field :link,  placeholder: "Insert Youtube / Souncloud / Website link" , class: 'u-full-width'%></p>

        <p><%= f.label :thoughts %>
        <%= f.text_area :thoughts,  placeholder: "Thoughts" , class: 'u-full-width'%></p>

        <p>Project it belongs to <%= f.collection_select(:project_id, Project.all, :id, :name) %></p>

  <p>
    <div class="form-group">
        <%= f.submit "Save" %>
</div>
</p>

<% end %>
</div>
    