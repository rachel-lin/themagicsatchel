<h1><%= @project.name %></h1>
<h3>Created on &middot; <%= @project.created_at.strftime("%d %b %Y") %> | 
Project status &middot; <%= @project.status %></h3>
<h4>
  <% if policy(@project).update? %>
   <%= link_to "Edit Project Details", edit_project_path, class: 'btn btn-success' %>
 <% end %> — 

 <% if policy(@project).destroy? %>
 <%= link_to "Delete Project", @project, method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this project and all of its related posts?' } %> 
 <% end %>
     </h4>
<div class="row">
  <%= render @texts %>
  <%= render @image_posts %>
</div>
