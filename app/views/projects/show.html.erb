<h2 class="project-title"><%= @project.name %></h2>
<h4>Created on &middot; <%= @project.created_at.strftime("%d %b %Y") %> <span class="typcn typcn-minus-outline"></span>
Project status &middot; <%= @project.status %></h4>
<h4>
  <% if policy(@project).update? %>
   <%= link_to  edit_project_path do %>
   <span class="typcn typcn-edit"></span>
   <% end %>
 <% end %> — 

 <% if policy(@project).destroy? %>
 <%= link_to @project, method: :delete, data: { confirm: 'Are you sure you want to delete this project and all of its related posts?'} do%> 
 <span class="typcn typcn-delete"></span>
 <% end %>
 <% end %>
     </h4>
<div class="row">
  <%= render @texts %>
  <%= render @image_posts %>
  <%= render @videos %>

</div>
